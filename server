const http = require('http');
const server = http.createServer((req, res) => {
    const url = req.url;
    if (url === '/') {

    }
    if (url === '/users') {

    }
    if (url === '/create-user') {

    }
});

server.listen(3000);
